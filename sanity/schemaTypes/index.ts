import { categoryType } from "./category";
import { heroImageType } from "./heroImage";
import { productType } from "./product";

export const schemaTypes = [productType, categoryType, heroImageType]
